<?xml version="1.0" encoding="utf-8"?>
<!-- ============================================================
     themes.xml
     Tema base da Activity. O Compose assume o controle da UI
     logo após o setContent{}, mas o tema XML é necessário para
     definir a aparência da janela antes disso (evita flash branco).
     Requer: com.google.android.material:material (em build.gradle)
     ============================================================ -->
<resources>

    <style name="Theme.Krono" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Fundo escuro enquanto o Compose carrega -->
        <item name="android:windowBackground">@android:color/black</item>

        <item name="android:windowIsTranslucent">false</item>

    </style>

    <!-- Tema para o Modo Foco — fullscreen sem ActionBar -->
    <style name="Theme.Krono.Focus" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

</resources>