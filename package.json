{
  "name": "krono",
  "version": "2.5.5",
  "private": true,
  "scripts": {
    "release": "python scripts/generate_changelog.py && commit-and-tag-version && git push --follow-tags origin main",
    "update-readme": "node scripts/update-readme.js"
  },
  "devDependencies": {
    "commit-and-tag-version": "^12.7.1"
  }
}
